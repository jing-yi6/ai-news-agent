name: ai-news-agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - pip
  # Core dependencies
  - requests>=2.31.0
  - python-dotenv>=1.0.0
  # LLM and scraping (via pip)
  - pip:
    - openai>=1.0.0
    - anthropic>=0.18.0
    - twscrape>=0.8.0
